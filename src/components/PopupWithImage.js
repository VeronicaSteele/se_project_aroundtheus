import Popup from "./Popup.js";
export default class PopupWithImage extends Popup {
  constructor() {}
  openModal(name, link) {
    //fill in the popup
    //image with the link from card
    //and the title with the name of the card
    super.openModal();
  }
}
